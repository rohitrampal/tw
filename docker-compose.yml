version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: twelfthkey-db
    environment:
      POSTGRES_USER: twelfthkey
      POSTGRES_PASSWORD: twelfthkey_password
      POSTGRES_DB: twelfthkey
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U twelfthkey"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: twelfthkey-app
    environment:
      DATABASE_URL: postgresql://twelfthkey:twelfthkey_password@postgres:5432/twelfthkey?schema=public
      NODE_ENV: production
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./.env.local:/app/.env.local:ro

volumes:
  postgres_data:

